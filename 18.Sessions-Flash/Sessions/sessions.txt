It's not very practical or secure to store a lot of data client-side using cookies.
This is where sessions comes in.

Sessions are server-side data store that we use to make HTTP stateful. Instead of storing data using cookies,
we store data on server-side and then send the browser a cookie that can be used to retrieve the data.

