// JavaScript Program to Convert the First Letter of a String into UpperCase
function capitalize(str){
    const capatalize = str[0].toUpperCase()+str.slice(1)
    console.log(capatalize)
}

capitalize("javascript")

